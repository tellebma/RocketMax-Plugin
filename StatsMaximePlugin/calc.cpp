#include "pch.h"
#include "calc.h"

std::string Calc::PlayerInfoToString(std::vector<PlayerInfo> playerInfo)
{
    std::string data = "j'y arrive pas";
    //std::string data = R"({"player_name": ")" + playerInfo.playerName +
    //    R"(", "player_id": ")" + std::to_string(playerInfo.playerId.GetUID()) +
    //    R"(", "elo": ")" + std::to_string(playerInfo.playerMMR) +
    //    R"("})";
    return data;
}

